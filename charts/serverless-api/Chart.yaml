apiVersion: v2
name: serverless-api
description: FaaS/CaaS REST API wrapping Knative on OpenShift
type: application
# Bumped by the release workflow (.github/workflows/release.yml) to the release
# version; between releases it holds the last released version (0.0.0 / "latest"
# before the first release). The container image tracks appVersion via the
# serverless-api.image helper.
version: 0.0.0
appVersion: "latest"
