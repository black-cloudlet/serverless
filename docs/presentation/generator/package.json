{
  "name": "serverless-platform-deck",
  "private": true,
  "description": "Generates the team presentation as an animated web deck and a PowerPoint file",
  "scripts": {
    "html": "node gen-html.js",
    "pptx": "node build2.js && node animate.js \"$PWD/deck-raw.pptx\" \"$PWD/serverless-platform.pptx\"",
    "build": "npm run html && npm run pptx"
  },
  "dependencies": { "pptxgenjs": "^4.0.0" }
}
